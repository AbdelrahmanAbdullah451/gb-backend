@model gb_raw.Models.Exam

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Exam</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SysUser.userName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SysUser.userName)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.examID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
